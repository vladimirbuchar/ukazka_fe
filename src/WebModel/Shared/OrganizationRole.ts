export default class OrganizationRole {
    public roleId: string;
    public roleIndentificator: string;
    constructor (roleId: string, roleIndentificator: string) {
      this.roleId = roleId
      this.roleIndentificator = roleIndentificator
    }
}
